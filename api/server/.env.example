# Server Configuration
NODE_ENV=development
PORT=3001

# JWT Secret - REQUIRED in production. Use a long random string.
JWT_SECRET=your-secret-key-change-in-production
JWT_EXPIRES_IN=7d

# CORS - comma-separated allowed origins (e.g. your frontend URL or same origin when serving both)
# For single deployment (frontend + backend together): set to your app URL, e.g. https://your-app.onrender.com
CORS_ORIGIN=http://localhost:8080,http://localhost:5173,http://localhost:3000

# Rate Limiting (optional)
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX=100
